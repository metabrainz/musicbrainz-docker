version: '3.1'

services:
  db:
    build:
      args:
        - POSTGRES_VERSION=9.5
    image: musicbrainz-docker_db:9.5
    command: /bin/sleep infinity

  musicbrainz:
    command: /bin/sleep infinity
