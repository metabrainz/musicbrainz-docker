This directory is expected to store your own compose overrides, and
any other related files (to build images, to configure a dockerized
program, to set environment variables, to create secrets, and so on).
