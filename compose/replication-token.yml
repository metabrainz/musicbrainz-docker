# Description: Grant access to MetaBrainz access token for replication

services:
  musicbrainz:
    secrets:
      - metabrainz_access_token

secrets:
  metabrainz_access_token:
    file: ${METABRAINZ_ACCESS_TOKEN_PATH:-local/secrets/metabrainz_access_token}
